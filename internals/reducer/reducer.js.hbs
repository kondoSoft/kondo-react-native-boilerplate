const initialState = {}

const {{ lowerCase name }} = (state, {type, payload}) => {
	switch (type) {
		case 'EXAMPLE_CASE':
		  return state
		default:
		  return state
	}
}

export default {{ lowerCase name }}